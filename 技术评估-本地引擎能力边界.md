# 本地引擎的能力边界 · 技术评估

> **2026-09 状态更新：本文的结论已被采纳，本地启发式评分引擎已下线。**
> 产品需求计划的三个待确认问题结论为：①本地评分**不保留** ②**采用开源方案** ③查重范围**暂不界定**。
> 本文从「要不要继续投入本地引擎」的论证，变为**为什么最终放弃它**的存档依据。
> 本地端保留下来的三类能力（文体门禁、相似度查重、证据核验）都不是给质量估值，而是做事实核查——详见 README 第五节。

---

> 起因：有人问「为什么不引一个现成的开源评分/NLP 库来增强自动化评分」。
> 本文记录**实测过程与数据**，结论是：**不是不想引，是浏览器端这条路在语义层面是死的。**
>
> 所有数据均可在本仓库复现（见文末「复现方式」）。

---

## 一、结论先说

1. **「只靠本地引擎不够用」这个判断成立** —— 但原因不是"没引好库"，
   而是**本地引擎的一切判断都来自一份手写正则词典，词典没写的表达它永远看不见**。
   这是**结构性**的，调参解决不了。
2. **引库救不了这个问题。** 浏览器端能塞进来的 NLP 库（TextRank / TF-IDF / 分词）
   都是**统计方法**，与本地引擎**同一层级** —— 换算法不换层级。
   要换层级必须上**语义模型**，而模型体积是这个「零依赖 + 离线 + 双击即用」架构承受不了的。
3. **真正该做的是让强引擎更容易被用上**，而不是把弱引擎做到「够好」。

---

## 二、实测数据

### 2.1 默认用的就是本地引擎

`app.js`：

```js
engine: U.store.get('engine', 'local'),   // ← 默认值就是 local
```

只有显式切到 `llm` **且配了 API Key** 才会走大模型；没配 Key 时静默降级回本地
（`app.js:164`，界面上的引擎徽标会如实显示实际用的引擎）。

**也就是说：评委打开链接、不配 Key，看到的天花板就是本地规则引擎。**

### 2.2 中文样例上的表现：合理

| 样例 | 字数 | 总分 | 等级 |
| --- | --- | --- | --- |
| 示例A · 高质量 | 1130 | **94.7** | A |
| 示例B · 中等质量 | 414 | **66.1** | C |
| 示例C · 疑似抄袭A | 718 | **80.6** | B |

排序与质量一致，A 的 8 个维度证据覆盖率 91~100%，B 只有 32~85%。**在词典覆盖到的范围内，它是自洽的。**

### 2.3 英文报告：31.4 分，但有警告

写了一份与示例A 质量相当的英文实验报告（248 词，含目的/环境/实现/数据表/图表/分析/问题/结论/参考文献）：

| | 中文示例A | 英文报告 |
| --- | --- | --- |
| 总分 | 94.7 | **31.4** |
| 等级 | A | **F** |
| 「问题与解决」维度 | 7.5/8 | **0/8** |

**但这不是静默的低分** —— 引擎检测到英文占比过高会主动出警告：

> 检测到本文档以英文为主（中文 0 字 / 英文 248 词）。本地启发式引擎的信号词典以中文撰写，
> 英文报告会大面积漏匹配，分数会明显失真——建议改用大模型。

这个处理是诚实的：**宁可说自己不行，也不给一个看起来客观的错分。**

### 2.4 漏判集中在少数信号

对 3 份中文样例统计各信号未命中次数：

```
   3 次  可复现说明
   2 次  涉及边界/异常处理
   2 次  给出解决过程
   2 次  总结避坑经验
   2 次  有参考文献
```

但**这 3 份样例是项目自造的，无法区分「真的没写」与「写法不在词典里」**。
要准确定位漏判，需要**真实作业语料 + 教师的人工修正记录**。

---

## 三、评估过的候选（含实测）

### `ChasLui/textrank4zh-ts` —— 结构完全合格，功能实测不适用

中文 TextRank（关键词提取 + 摘要）。逐项体检：

| 检查项 | 结果 |
| --- | --- |
| 许可证 | **MIT** ✅ |
| 运行时依赖 | **零** ✅ |
| 浏览器可用 | 有 IIFE 构建，全局 `TextRank4ZH` ✅ |
| 自包含性 | 无 `import` / `require` / WASM / `fetch` / XHR ✅ |
| 体积 | **140.7 KB**（gzip 35 KB）、6627 行 |
| 外部模型 | 不需要，自带 2658 条中文词表 ✅ |

**每一项都通过了。** 但拉下来真跑，在技术中文上输出的是垃圾。
同一段约 300 字的哈希表实验报告，三种方案对比：

| 方案 | 抽出的关键词 | 体积 |
| --- | --- | --- |
| **本项目已有的 `induce.extractTerms`** | 哈希表 负载因子 数据规模 阈值 扩容 查找 耗时 | **0（已在库内）** |
| TextRank + 内置词表 | **值 存 时 性 实验 希 率 数 平均 化 度 表** ← 全是单字 | 140.7 KB |
| TextRank + n-gram 分词注入 | 哈希表 数据规模 扩容 负载因子 性能 **希表** ← 有碎片噪声 | 140.7 KB |

库自己的 README 就承认：「词表外的中文会退化为逐字切分」。**2658 条日常词表对技术中文远远不够。**

**结论**：我们已有的 `extractTerms`（n-gram 2–4 + 停用词 + **凝固度剪枝**）
输出质量不输注入方案，还少一片碎片噪声，且零额外体积。

### 其他方向的排查

| 方向 | 为什么不行 |
| --- | --- |
| `yiming-liao/zhq`（BM25 + jieba） | 需加载 **jieba WASM（约 2 MB）**，`file://` 下取不到 |
| `Spencerx/wink-nlp`（约 10 KB） | 语言模型文件另计（约 1 MB），且 POS/NER 主要面向英文 |
| `kyr0/vectorstore`（浏览器内向量检索） | 需下载 **约 500 MB** 嵌入模型 |
| 中文词向量 / 小型 transformer | 量化到最小也要数 MB；base64 后单文件版从 506 KB 涨到数 MB，**「双击秒开」直接失去意义** |

---

## 四、为什么引库救不了：能力层级

| 层级 | 代表手段 | 能做什么 | 能不能塞进本项目 |
| --- | --- | --- | --- |
| **统计** | 词频、n-gram、TextRank、TF-IDF、BM25 | 数出哪些词常出现、彼此共现 | ✅ 能，但**和现有引擎同级** |
| **句法** | 分词 + 词性 + 依存分析 | 知道"误差"是主语还是宾语 | ⚠️ 需词典或模型，中文在浏览器端没有可用方案 |
| **语义** | 词向量、句向量、transformer | 知道「准确度」和「精度」是一个意思 | ❌ 模型体积与架构冲突 |

> **本地引擎的全部短板都在「语义」那一层：分不清同义改写、判断不了论述深浅。**
> 而统计层的库再多也上不去 —— 这是换算法不换层级。

已经在用的统计层手段（`induce.js` 的 n-gram + 凝固度剪枝、`analyzer.js` 的信号正则、
`consensus.js` 的多引擎共识）**已经把这一层的收益基本吃干**。

---

## 五、三条路，怎么选

| | 做法 | 收益 | 代价 |
| --- | --- | --- | --- |
| **A** | **保持零依赖，把力气放在「不需要语义」的能力上** | 结构完整性、数据密度、体裁判定、相似度、**篇幅偏差**、**评分溯源**、**批次覆盖** —— 这些本地引擎做得很好 | 语义类问题（同义改写、论述深浅）永远解决不了 |
| **B** | **引入模型，放弃「离线双击即用」** | 真正换层级，语义问题可解 | 破掉参赛的核心卖点；单文件版从 506 KB 变成数 MB |
| **C** | **补手写词典（同义表达扩展）** | 提升命中率，成本低 | 边际收益递减；且**必须有真实作业语料**才能做对，自造样例无法定位漏判 |

### 推荐：A 为主体 + C 用真实语料做 + B 作为「可选增强路径」

1. **主体走 A**。第 7 节的六个可信度指标（含最近新增的篇幅偏差、评分溯源、批次内容覆盖）
   全部落在这一层 —— 它们回答的是「分数可不可信、怎么来的」，
   **不需要语义理解也能做**，而且这正是教师真正关心的问题。
2. **C 不要靠 AI 拍脑袋造同义词。** 正确做法是**人在回路**：
   记录教师对自动分的人工修正，从「机器给 X / 教师改成 Y」的差异里长出词典与权重。
   （这正是 README 第 12 节「后续规划」的第一条。）
3. **B 保留为可选路径**，并在界面上**明确告知当前用的是哪个引擎**（已实现），
   让用户在「快而弱」与「慢而强」之间自己选。

---

## 六、一句话回答「为什么不引现成库」

> 评估过 `textrank4zh-ts`：MIT、零运行时依赖、IIFE 自包含、体积 140 KB —— **结构上完全合格**。
> 但拉下来在技术中文上实测，抽出的是「值 存 时 性」这类单字垃圾。
> 我们已有的 n-gram + 凝固度剪枝实现质量相当且体积为零。
>
> 更根本的是：**浏览器端能塞进来的都是统计方法，与本地引擎同一层级；能换层级的语义模型塞不进来。**
> 所以这条路不是「还没找到合适的库」，而是**在当前架构约束下不存在合适的库**。
> 要突破就得放弃「零依赖、离线、双击即用」—— 那是一个产品决策，不是技术选型。

---

## 复现方式

```bash
# 中文样例评分与漏判分布
node _design/measure-gaps.mjs

# 英文报告表现（含语言警告）
node _design/test-english.mjs

# 三种关键词抽取方案对比
node _design/compare-terms.mjs
```

（`_design/` 为评估用的临时脚本目录，不入库；上述结论所需的样本与代码均已内联在脚本中。）

---

*本评估涉及的第三方项目：`ChasLui/textrank4zh-ts`（MIT）—— 仅作评估，未引入本项目代码。*
