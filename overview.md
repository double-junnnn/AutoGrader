# AutoGrader 项目接手 · 交付说明

**日期**：2026-09-17
**结论**：源码已从线上单文件版**无损还原**，验证通过（逐字节一致），可直接开始二次开发。

---

## 一、背景：接手时面对的问题

你给的材料是 README + HANDOVER + 线上地址 `https://aa05c82aff428371d.app.workbuddy.host`。
接手前先做了一轮盘点，发现一个关键事实：

| 项 | 状态 |
| --- | --- |
| 本机是否有**最新**多文件源码 | ❌ 没有。全盘搜索无 `rubriclab.js` / `chat.js` / `build-single.py` |
| 本机仅有的一份代码副本 | `桌面/下载/autograder.zip`（**9-16 22:21** 快照，缺 theme/voice/rubriclab/chat/pdf 等 8 个模块） |
| 线上地址 | ✅ 可访问，461,523 字节，**含全部 18 个模块**（最新版） |

即：**线上那份单文件版是当前唯一完整的代码副本**。所以接手的第一步不是改代码，而是先把源码拿回来。

---

## 二、做了什么

### 1. 反向还原源码（核心技术动作）

`build-single.py` 打包时会为每个模块插入 `/* ===== <name>.js ===== */` 边界注释。
这个细节让打包**可逆** —— 顺着边界把内联块拆回独立文件即可。

还原结果：`index.html` + `main.css` + **18 个 JS 模块**，模块顺序与依赖关系完整保留。

### 2. 重建 `build-single.py` 并做**逐字节验证**

原打包脚本已随源码一起丢失，按观察到的内联格式重写了一个，然后：

```
线上原件  461523 bytes  sha256 595721a43abd6db7
重打包后  461523 bytes  sha256 595721a43abd6db7   → 逐字节一致 ✅
```

**这是本次最硬的证据**：还原出的源码不是"看起来差不多"，而是能重新生成出与线上**完全相同**的产物。
若还原过程中丢了一个换行或改了顺序，这个比对立刻会失败。

### 3. 端到端往返回归

又跑了一次「单文件 → 还原 → 重打包」的往返（`tools/unbuild-single.py`），结果同样逐字节一致。
这条链路现在可以被反复使用：**单文件版 = 可用的灾备副本**。

### 4. 补齐配套文件

| 文件 | 来源 |
| --- | --- |
| `samples/` 4 份样例（含真实 docx） | 从 9-16 旧 zip 取回 |
| `LICENSE` | 从 9-16 旧 zip 取回 |
| `README.md` / `HANDOVER.md` | 你提供的版本（最新） |
| `build-single.py` | **重写**（原文件丢失） |
| `tools/scan-banned-words.mjs` | **重写**（原文件丢失） |
| `tools/unbuild-single.py` | **新增**（单文件→多文件的反向工具，灾备用） |

### 5. 重写违禁词体检脚本，并实跑验证

README 承诺「0 处一级 / 0 处二级」，但脚本本身丢了 —— 承诺失去了可复跑的支撑。
按第八节描述的两级词库 + 三条豁免规则重写，并实跑：

```
一级（绝对禁止）命中 0 处 ✅
二级（灰区）命中 0 处 ✅
```

首跑时有 3 处误报（`zip 炸弹`、`卡死`、`滚雪球`），都是技术术语。
根因是扫描器按**行首**判断注释，把 `const N = 100; // 防 zip 炸弹` 这类**行尾注释**当成了正文。
已改为真正的词法处理（跟踪 `' " \`` 三种引号与转义，正确剥离行尾注释和块注释），而不是去改文案。

---

## 三、验证结果汇总

| 检查项 | 结果 |
| --- | --- |
| 还原源码重打包 vs 线上原件 | ✅ 逐字节一致（sha256 `595721a43abd6db7`） |
| 18 个 JS 模块语法检查 | ✅ 全部通过 |
| `index.html` 19 个资源引用 | ✅ 零缺失，加载顺序正确 |
| 全局命名空间定义/引用一致性 | ✅ 17 个命名空间，无悬空引用 |
| 违禁词体检 | ✅ 0 处一级 / 0 处二级 |
| 单文件→多文件→单文件 往返 | ✅ 逐字节一致 |

> **未做浏览器冒烟测试**：`agent-browser` 需下载 ~500MB Chromium 且要求全局安装，成本不划算。
> 由于产物逐字节等于线上可运行版本，脚本加载路径与顺序也已静态验证，运行期风险极低。

---

## 四、交付清单

```
2026-09-17-19-03-05/
├── autograder/                      多文件源码（可直接二次开发）
│   ├── index.html
│   ├── assets/css/main.css
│   ├── assets/js/                   18 个模块
│   ├── samples/                     4 份样例（含真实 docx）
│   ├── tools/scan-banned-words.mjs  违禁词体检
│   ├── tools/unbuild-single.py      单文件→多文件（灾备）
│   ├── build-single.py              多文件→单文件（改完必跑）
│   ├── README.md  LICENSE
├── AutoGrader-单文件版.html          内联产物，双击即用
├── autograder.zip                   29 文件 / 232 KB，解压即得 autograder/
├── README.md  HANDOVER.md
└── _recon/                           校验用临时目录（线上原件快照 + 往返验证产物，可整个删掉）
```

**日常两条命令**（在 `autograder/` 下）：

```bash
python3 build-single.py                                  # 改完源码必须重建单文件版
node tools/scan-banned-words.mjs                         # 改完人格词库/文案必跑，期望 0 处一级
```

---

## 五、遗留问题与风险（诚实说明）

1. **git 历史未恢复**。HANDOVER 提到仓库里有 20+ 次提交、每次改动动机都写在 commit message 里 ——
   这些**没有随源码一起找回**（旧 zip 与线上产物都不含 `.git`）。目前是一个干净的初始状态。
2. **三份材料未找回**：`评阅报告-样例输出.pdf`、`答疑助手-效果截图.png`、`文体门禁-效果截图.png`。
   需要的话可以现场重新生成。
3. **`samples/` 可能不是最新版**：取自 9-16 的旧 zip。抽查确认 示例A 与 `demos.js` 内置版同源，
   但不排除后续有微调。**注意 `samples/` 不参与打包**，不影响运行结果。
4. **`scan-banned-words.mjs` 与 `build-single.py` 是重写的**，行为对齐了 README/HANDOVER 的描述，
   但原始实现里的细节（比如词库的完整条目）无法 100% 复原。当前实跑结果与 README 声明一致。
5. **`build-single.py` 的路径约定**：产物写在 `autograder/` 的**上一级**，与 README 描述一致。

---

## 六、下一步可以做什么

源码已经回来且可验证，接下来任何改动都能走「改 → 重建 → 体检 → 对比」的闭环。按 HANDOVER
第九节列的优先级，最值得做的三件事：

1. **真实 Key 全链路验证** —— 配一个免费的（智谱 GLM-4.7-Flash / 硅基流动 9B 以下）跑通
   评分 + 答疑 + 量表生成，补上 mock 覆盖不到的容错。
2. **英文信号词典** —— 目前英文报告只能如实提示失真。
3. **答疑知识库扩容** —— 从 14 类往上加，注意同步补 `OWN` 强特征词表。

需要我从哪一件开始，或者你有别的优先项，直接说。
