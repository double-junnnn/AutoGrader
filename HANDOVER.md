# AutoGrader 交接说明 · 给接手的 AI

> **怎么用这份文档**：把它整份贴给你的 AI 助手，再让它读 `autograder/` 目录。
> 文档自包含——**不读代码也能理解八成**，读完代码就能直接接手改。
> 最后两节（「别推翻的决策」「已知坑」）是重点，那里面全是踩过的坑，一条都别重踩。

---

## 0. 三十秒速览

AutoGrader 是一个**中文实验报告智能评阅 Web 应用**，给高校教师用：拖入一批学生实验报告，
自动出分、出评语、查重、导出成绩表与 PDF。

**参赛作品**：粤港澳大湾区 AI Coding 创新大赛 · 方向一（AI + 教学管理助手）。

最硬的约束：**零依赖、可离线**——无框架、无构建、无 CDN、无后端，
解压后双击 HTML 就能用。这一条决定了后面几乎所有技术选型，动它之前先读完第 4 节。

---

## 1. 材料包清单（迁移时一并带走的）

| 材料 | 路径 | 用途 |
| --- | --- | --- |
| **本文档** | `HANDOVER.md` | 给接手 AI 的上下文 |
| 完整源码 | `autograder/` | 多文件版，二次开发用 |
| 详细文档 | `autograder/README.md` | 评分口径、算法、各功能设计说明（约 500 行，最全） |
| 可运行成品 | `AutoGrader-单文件版.html` | CSS 与全部 JS 已内联，**双击即用**，看效果最快 |
| 打包 | `zip -rq autograder.zip autograder` | 现打即可，仓库里刻意不存 zip 副本（与源码 100% 重复，只会和源码脱节） |
| 效果样例 | `评阅报告-样例输出.pdf` | 导出 PDF 长什么样 |
| 效果截图 | `答疑助手-效果截图.png`、`文体门禁-效果截图.png` | 答疑与离题拦截的实际界面 |
| 体检脚本 | `autograder/tools/scan-banned-words.mjs` | 违禁词扫描，改人格词库后必跑 |
| 回归套件 | 已移除（见第 7 节） | 原 18 个脚本，交付精简时删除；`git checkout 9b9c537 -- autograder/tools/tests` 可取回 |
| git 历史 | 仓库内 20+ 次提交 | 每次改动的动机都写在 commit message 里 |

**源码改完后必须重建单文件版**：`cd autograder && python3 build-single.py`
（单文件版是内联产物，不重建就会和源码脱节——这个错犯过）。

---

## 2. 现在能干什么（功能清单）

1. **多格式解析**：`.md` / `.txt` / `.docx` / `.pdf`（PDF 解析引擎按需联网加载一次）
2. **本地评阅**：8 维度启发式引擎，没 API Key 也能出分，**每一分都可解释**
3. **大模型评阅**：OpenAI 兼容接口，本地/大模型双引擎可交叉验证
4. **智能量表 Rubric Lab**：一句话描述评分偏好 → 生成量表；或按这批作业自动适配分值；
   7 套学科模板打底；手动调节始终开放且优先级最高
5. **离题文档拦截**：上传小说/散文/乱码 → 判 0 分并**逐条列出判定依据**；判错可一键申诉
6. **质量三件套**：诱导注入自检、评分信度复算（Bootstrap）、双引擎交叉验证
7. **抄袭检测**：字符 5-gram Jaccard，带相似度热力图
8. **批量**：一次评多份，导出成绩表 CSV / Markdown / PDF
9. **零依赖 PDF 导出**：Canvas 手绘排版 + 自封装 PDF 字节流，不引任何库
10. **3 套主题 × 3 档评语人格**：动画卡通（明黄底）/ 浅玻璃（白蓝 visionOS）/ 深玻璃（深蓝紫 visionOS）；学院派 / 傲娇小天才 / 火力全开
11. ~~**英文文档如实提示失真**，不装作评得准~~ —— 该限制随本地引擎下线消失，评分统一走大模型后不再有语言告警
12. **站内答疑助手**（最新）：右下角对话框，本地知识库优先，配 Key 后转大模型并带评分上下文

---

## 3. 代码地图

```
autograder/
├── index.html                 单页骨架，所有视图与弹窗
├── assets/css/main.css        全部样式（3 套主题靠 CSS 变量切换）
├── assets/js/
│   ├── utils.js               工具层：DOM、存储、事件总线、归一化
│   ├── parser.js              特征提取（字数、章节、代码块、图表…）
│   ├── rubric.js              量表模型与等级换算
│   ├── rubric-templates.js    7 套学科量表模板（含各维度考察要点）
│   ├── rubriclab.js           智能量表：意图解析 + 批次自适应 + 混批检测
│   ├── analyzer.js            评分解算：维度打分、门禁、总评
│   ├── induce.js              诱导注入自检
│   ├── reliability.js         评分信度复算
│   ├── consensus.js           双引擎交叉验证
│   ├── voice.js               ★ 三档人格词库 + system prompt（评语与答疑同源）
│   ├── chat.js                ★ 站内答疑引擎：知识库 + 匹配 + 上下文注入
│   ├── llm.js                 ★ 大模型引擎：鉴权、错误处理、结构化输出降级
│   ├── docx.js / pdf.js       导入 / 导出（PDF 为自研零依赖实现）
│   ├── charts.js              手写 SVG 图表
│   ├── theme.js               主题切换
│   ├── demos.js               内置示例报告
│   └── app.js                 主逻辑与渲染（约 1800 行，最大的一个文件）
├── tools/scan-banned-words.mjs  违禁词体检
└── build-single.py             内联打包成单文件 HTML
```

模块间通过全局 `window.AG` 命名空间通信（`AG.utils`、`AG.voice`、`AG.chat`、`AG.llm`…），
每个文件是 IIFE。**这是刻意的**，原因见下一节。

---

## 4. 别推翻的决策（每一条都有代价）

| 决策 | 为什么 | 推翻会怎样 |
| --- | --- | --- |
| **IIFE + 全局命名空间，不用 ES Module** | ES Module 在 `file://` 下会被 CORS 拦截，双击打不开 | 直接毁掉「双击即用」这个核心卖点 |
| **不用任何框架、不引 CDN** | 离线可用、评委零等待 | 同上，且引入构建链风险 |
| **无后端** | 部署即拷贝；隐私（报告不出本机） | 需要服务端代理，卖点没了 |
| **PDF 自己写（Canvas + 字节流）** | 引库就破坏零依赖 | 要么引库，要么手写 300 行 |
| **答疑：本地知识库优先** | 教师问「为什么这份 19 分」，答案取决于**这份报告的实际扣分**，大模型看不到，只会回「可能内容不够充实」这种正确的废话 | 联网依赖 + 答非所问 |
| **人格硬约束 5 条，且写进 prompt** | 用户明确要求「收敛、不用违禁词」；不写进 prompt 模型不知道边界 | 生成越界内容 |
| **`response_format` 只在该要 JSON 时开** | 见第 5 节第 1 条 | 连通性测试与答疑必然 400 |
| **英文报告如实提示失真** | 本地信号词典是中文的，英文会大面积漏匹配，给个看似客观的低分是骗人 | 分数不可信 |

---

## 5. 已知坑（都是真踩过的）

**1）`response_format: json_object` 不能无条件带。** OpenAI 规定该模式下 `messages`
里必须出现 json 字样，否则 400。连通性测试（「请只回复 OK」）和答疑（自然语言提问）
的 prompt 都没有这个词 → 必然失败，且表现为「Key 和地址都对但就是连不上」。
现在只有评分和量表生成开这个参数，且带自动降级（对方拒绝时去掉参数重试一次）。
**改 `llm.js` 时别把它加回去。**

**2）`file://` 下 `blob:` 下载会被浏览器拦。** 所以导出走 `a[download]` + 环境检测，
受限时给出替代方案提示。别改成 blob 直下。

**3）`state.currentId` 不落 localStorage**，刷新后由 `init()` 兜底到第一份。
写测试脚本想读「当前选中的报告」时会读到 null——那是脚本的问题，不是 bug。

**4）`pkill -f <名字>` 在 zsh 里会把执行它的 shell 自己也匹配杀掉**（命令行含同样字符串）。
换端口绕开，别用 pkill。

**5）~~本地引擎对英文报告必然失真~~（已失效）**：那条限制的前提（中文信号词典打分）已随本地引擎下线消失，
英文报告现在与中文走同一条模型链路，不再弹语言告警。剩下的英文相关限制只有两条：
模型自身的英文评阅质量，以及评语人格的中文措辞是按中文报告调的。

**6）人格词库改完必须跑违禁词扫描**：`node tools/scan-banned-words.mjs`。
一级命中即打回。脚本豁免注释行、禁令语境行（prompt 里必须点名禁什么）以及「写死」
这类技术术语。

---

## 6. 用户偏好（协作方式）

- **全程中文**回复，代码注释也中文。
- 给需求时常附**长文本素材**或**截图**（比如整份台词数据集、报错弹窗），要从中提炼意图，
  不是照抄。
- 明确要求过：**不要用违禁词**、风格要收敛。这类硬约束要落成**可复跑的检查**，
  而不是口头承诺。
- 喜欢**有观点**的建议：给方案时说清推荐哪个、为什么，别列一堆选项让他自己挑。
- 交付标准（已形成的默契）：改完要
  ① 跑全量回归（多文件版 + 单文件版 `file://` 双份全绿、零运行期错误）
  ② 违禁词扫描 0 处一级
  ③ 重建单文件版 / 样例 PDF / zip
  ④ 更新 README 并提交 git。

---

## 7. 怎么验证你没改坏

```bash
cd autograder
node tools/scan-banned-words.mjs        # 违禁词：期望 0 处一级
python3 build-single.py                 # 源码改完后必须重建单文件版
cp ../AutoGrader-单文件版.html /tmp/isolated/
```

体检与打包是唯一留在仓库里的两条命令，改完人格词库或源码务必跑：

```bash
node tools/scan-banned-words.mjs        # 违禁词：期望 0 处一级
python3 build-single.py                 # 源码改完后必须重建单文件版
```

> **回归套件已不在仓库里**（交付精简时主动移除，共 18 个脚本：12 个 Playwright 套件 +
> `mock_openai.py` 假服务商 + `gen_pdf.mjs` 样例生成 + `scan_mojibake.mjs` 乱码扫描）。
> 需要时从 git 历史取回即可：`git checkout 9b9c537 -- autograder/tools/tests`。
> 那个版本全部改造为可移植写法（路径按脚本自身位置推导，Playwright 三级回退查找），
> 最后一次全跑的结果是 12/12 通过、违禁词 0 处、乱码 0 处。

### 没有测试脚本时怎么保命

改代码后至少手工过一遍这四件事，它们覆盖最容易炸的地方：

1. **双版本都打开**——单文件版是内联产物，打包器正则偶尔会漏文件，务必重跑 `build-single.py`
2. 点「加载示例」→ 出 3 份评分 → 切「批量与查重」看相似度 → 导出 PDF
3. 上传一份小说之类的非作业文档，确认被文体门禁判 0 分并列出依据
4. 配一个 OpenAI 兼容地址后先点「测试连通性」，再评一份

---

## 8. 待办与未决

- **交付物没有任何外部托管**，全部是本地静态文件；如需上线，整个目录传到任意静态托管即可，
  没有构建步骤、不需要环境变量或后端服务。
- 大模型链路**只有 mock 与 stub 验证过**，没跑过真实 Key 的完整评分——
  因为开发环境里没有 Key。首次真实使用时建议先点「测试连通性」再评一份。
- 英文报告支持：未做，见第 5 节第 5 条。
- 答疑知识库是**离线快照**，宁可说「匹配不到」也不编——这是设计，不是缺陷。

---

## 9. 如果你要继续做，最可能接手的三个方向

1. **真实 Key 全链路验证**：配一个 DeepSeek 或智谱的免费 Key，跑一遍评分 + 答疑 + 量表生成，
   把 mock 覆盖不到的真实差异（比如模型输出 JSON 但不合格式）补上容错。
2. **英文信号词典**：现在英文报告只能如实提示失真，做一套英文信号词就能真正支持。
3. **答疑知识库扩容**：现在 14 类，可按需加，加的时候记得往 `OWN` 强特征词表里补词
   ——只按词长加权会把主体词压过去（「为什么是 0 分」会被归因条目抢走）。

改之前先读 `autograder/README.md` 对应章节，那里的设计说明比代码注释详细。
